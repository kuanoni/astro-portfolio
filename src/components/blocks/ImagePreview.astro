---
import '@styles/components/ImagePreview.scss';

interface Props {
	src: string;
	alt: string;
}

const { src, alt } = Astro.props;
---

<div class='thumbnail'>
	<img src={src} alt={alt} />
	<span>
		<img src={src} alt={alt} />
	</span>
</div>
