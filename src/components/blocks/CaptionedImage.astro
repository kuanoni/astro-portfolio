---
interface Props {
	src: string;
	alt: string;
	caption: string;
}

const { src, alt, caption } = Astro.props;
---

<figure>
	<img src={src} alt={alt} />
	<figcaption>{caption}</figcaption>
</figure>
